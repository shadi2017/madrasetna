# تشغيل مدرستنا على حساباتك المجانية

## 1. Supabase
أنشئ مشروعًا جديدًا مخصصًا للتطبيق داخل الخطة Free، واحتفظ بكلمة مرور قاعدة البيانات عندك.

1. من SQL Editor افتح New query والصق محتوى supabase/migrations/001_initial.sql ثم Run. الملف مخصص **لمشروع جديد** ويُشغّل مرة واحدة.
2. من Authentication → Providers/Sign In أوقف السماح بالتسجيل العام (Allow new users to sign up). الأدمن هو الذي ينشئ حسابات الطلاب.
3. من Authentication → Users أضف حسابك كأول أدمن ببريدك وكلمة مرور قوية، وفعّل تأكيد البريد.
4. شغّل supabase/bootstrap-admin.sql بعد استبدال البريد التجريبي ببريدك. لا يوجد باسورد أدمن افتراضي داخل المشروع.
5. من Project Settings → API احصل على Project URL وPublishable key أو anon public key.

لا تُرسل service_role أو secret key أو كلمة مرور قاعدة البيانات في المحادثة، ولا تضعها في GitHub أو في أي متغير يبدأ بـ VITE_.

## 2. وظيفة إدارة الحسابات
من لوحة Supabase → Edge Functions أنشئ وظيفة باسم manage-student والصق supabase/functions/manage-student/index.ts وانشرها.
أوقف خيار Verify JWT باستخدام legacy secret لهذه الوظيفة فقط، وفق config.toml. الوظيفة نفسها تتحقق من توكن المستخدم عبر auth.getUser، ثم تتأكد من عضوية الأدمن في قاعدة البيانات قبل أي عملية. لا تعتمد على فك توكن غير موثّق.
المتغيرات SUPABASE_URL وSUPABASE_ANON_KEY وSUPABASE_SERVICE_ROLE_KEY تكون متاحة في بيئة Edge Functions المدمجة؛ لا تنقل مفتاح الخدمة إلى الواجهة.

بديل بالأداة الرسمية:
```sh
npx supabase login
npx supabase link --project-ref YOUR_PROJECT_REF
npx supabase functions deploy manage-student --no-verify-jwt
```
لا تستخدم db push بعد تشغيل SQL يدويًا إلا بعد مواءمة سجل الهجرات؛ اختَر طريقة واحدة لتطبيق الهجرة.

## 3. ربط الواجهة
أنشئ .env.local بجانب package.json:
```env
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_PUBLISHABLE_OR_ANON_KEY
```
المفتاح العام مسموح ظهوره في الواجهة؛ الحماية تأتي من سياسات قاعدة البيانات. أعد تشغيل المعاينة أو البناء بعد تغييره.

## 4. النشر المجاني على GitHub Pages
1. أنشئ repository في حسابك وارفع المشروع دون node_modules أو .env.local أو أي مفاتيح خاصة.
2. من Settings → Secrets and variables → Actions → Variables أضف VITE_SUPABASE_URL وVITE_SUPABASE_ANON_KEY.
3. من Settings → Pages اجعل Source = GitHub Actions.
4. ارفع الفرع main أو شغّل Deploy Madrasetna يدويًا من Actions.
5. بعد نجاح النشر سيظهر رابط Pages. افتحه من الموبايل وسجل دخول الأدمن بالبريد.
6. أضف عنوان الموقع إلى Authentication → URL Configuration في Supabase كـ Site URL. لا توجد redirects للدخول بكلمة المرور، لكن ضبطه مطلوب لأي إعدادات بريد مستقبلية.

الاستضافة لا تحتاج خادم Node دائمًا؛ الملفات الناتجة في dist.

## بديل: Cloudflare Pages
اربط repository الخاص بك، واستخدم:
- Build command: npm run build
- Output directory: dist
- Node version: 24
- نفس متغيري VITE العامين أعلاه.
المشروع يستخدم روابط نسبية ليدعم نطاقًا رئيسيًا أو مسار repository في GitHub Pages.

## 5. تحقق قبل إدخال بيانات الطلاب
- سجل كأدمن وأضف طالبين للتجربة، وسلّم كل واحد اسم المستخدم والباسورد.
- افتح طالبًا في متصفح منفصل: يجب ألا يظهر له غير بروفايله.
- حدد تاريخ يوم الدراسة. اسمح بالكاميرا وامسح QR الطالب من موبايل آخر؛ المسح المكرر يجب أن يقول إن الحضور مسجل.
- غيّر درجة من حساب الأدمن وتأكد من وصولها لصفحة الطالب.
- جرّب حذف طالب واسترجاعه، ثم تغيير كلمة مروره.
- صدّر ملف Excel، وغيّر اسم طالب أو تقييمًا، ثم استورده بعد مراجعة المعاينة.
- اختبر Safari على iPhone وChrome على Android عبر HTTPS. زر إضافة للشاشة الرئيسية موجود في قائمة مشاركة Safari أو قائمة Chrome حسب الجهاز.
اختبارات المشروع المحلية لا تستبدل التحقق من إعداد Supabase والكاميرا على أجهزة فعلية.

## النسخ الاحتياطي والتكلفة
نزّل ملف Excel دوريًا واحتفظ به في مكان خاص. سجل التدقيق يساعد في التراجع لكنه ليس نسخة احتياطية مستقلة من قاعدة البيانات أو من حسابات الدخول.
بحسب الأسعار التي تمت مراجعتها أثناء البناء: Supabase Free يتضمن 500 MB لقاعدة البيانات و200 اتصال Realtime متزامن. المشروع المجاني قد يتوقف بعد 7 أيام نشاط منخفض، ويحتاج استئنافًا من لوحة التحكم. الاستخدام الكبير قد يحتاج خطة أخرى؛ لا يوجد وعد باستضافة مجانية غير محدودة.
المراجع: https://supabase.com/pricing و https://supabase.com/docs/guides/platform/free-project-pausing و https://developers.cloudflare.com/pages/platform/limits/

## دليل الاستخدام
- أول مرة: الإعدادات → اسم/شعار/آية/لوجو، ثم أيام الدراسة → إضافة 10 أيام أو الأيام التي تريدها.
- الطلاب → إضافة طالب. اسم المستخدم بحروف إنجليزية صغيرة وأرقام وشرطة سفلية، والموبايل 8–15 رقمًا مع + اختياري.
- التقييمات → طالب + يوم → الحضور وبنود التقييم → حفظ.
- الحضور قيمته 10. كل من الالتزام والكتاب المقدس والخلوة والحفظ والموبايل والترانيم والألعاب والكويز والمسابقة والمشروع من 10؛ مجموع اليوم 110.
- السجل → تفاصيل التعديل ثم استرجاع السابق، أو المحذوفات → استرجاع.
- استيراد وتصدير → القالب أو التصدير. احتفظ بالمعرف والإصدار عند تعديل سجل موجود. لا تعدل رمز QR أو اسم مستخدم موجود.
- الاستيراد صفًا بصف وليس عملية واحدة: التقرير يوضح الصفوف التي نجحت والتي فشلت. بعد التصحيح أعد استيراد الفاشل فقط من تصدير حديث.
- الحسابات الجديدة عند الاستيراد لها كلمات مرور مولدة، تُنزّل من الكشف الخاص الظاهر قبل مغادرة الصفحة.

